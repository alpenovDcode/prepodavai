# 🚀 Быстрый тест GigaChat на продакшене

## 1️⃣ Проверка .env файла

```bash
# На сервере проверьте, что GigaChat переменные установлены
cat .env | grep GIGACHAT

# Должны быть:
# GIGACHAT_CLIENT_ID=...
# GIGACHAT_CLIENT_SECRET=...
# GIGACHAT_DISABLE_TLS_VERIFICATION=false
```

## 2️⃣ Проверка контейнеров

```bash
# Проверьте, что backend запущен
docker ps | grep backend

# Проверьте логи на ошибки
docker logs prepodavai-backend-prod --tail 50 | grep -i error
```

## 3️⃣ Быстрый тест через API

Замените `YOUR_DOMAIN` и `YOUR_TOKEN` на реальные значения:

```bash
# Тест 1: Получить список моделей
curl -X GET https://YOUR_DOMAIN/api/gigachat/models \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json"

# Ожидается: {"success": true, "models": {...}}
```

## 4️⃣ Тест через веб-интерфейс

1. Откройте ваш домен
2. Авторизуйтесь
3. Выберите **"GigaChat"**
4. Режим: **"Текстовая беседа"**
5. Промпт: `"Привет, как дела?"`
6. Нажмите **"Создать"**
7. Дождитесь результата (10-30 сек)

## 5️⃣ Проверка логов

```bash
# Смотрите логи в реальном времени
docker logs prepodavai-backend-prod -f

# Ищите:
# ✅ GigaChat token получен
# ✅ GigaChat generation completed
```

## ✅ Успех, если:

- [ ] `GET /api/gigachat/models` возвращает список моделей
- [ ] `POST /api/gigachat/generate` создаёт запрос
- [ ] Генерация завершается со статусом `completed`
- [ ] Результат виден в истории

## 🐛 Если не работает:

```bash
# 1. Проверьте переменные в контейнере
docker exec prepodavai-backend-prod env | grep GIGACHAT

# 2. Перезапустите backend
docker compose restart backend

# 3. Проверьте логи
docker logs prepodavai-backend-prod --tail 100
```

## 📚 Подробная документация

- Полное тестирование: [PRODUCTION_TESTING.md](./PRODUCTION_TESTING.md)
- Настройка .env: [PRODUCTION_ENV_SETUP.md](./PRODUCTION_ENV_SETUP.md)

